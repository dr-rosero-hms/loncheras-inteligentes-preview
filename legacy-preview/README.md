# legacy-preview

Extras del preview original (Cloudflare Pages) que nunca llegaron a producción en SiteGround: `test-extended.js`, `setup-secrets.sh` y el experimento `test-3-1/`. Se conservan solo como referencia histórica.
